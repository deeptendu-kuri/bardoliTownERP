# Frontend (safe to expose — RLS protects data)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only secrets — set via `supabase secrets set`, NEVER in the client bundle
# ANTHROPIC_API_KEY=
# WHATSAPP_TOKEN=
# WHATSAPP_PHONE_ID=
